console.log('5' + 10);

/* The code written above will display '510' instead of 15 because
'5' is a string and 10 is a number. When a string is paired to a number
with the + operand, JavaScript will concat them instead of adding values.
