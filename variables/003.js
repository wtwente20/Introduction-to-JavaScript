{
  let foo = 'bar';
}

console.log(foo);

/* foo is a locally declared variable, so trying to access it globally 
is impossible because of scope. foo can only exist inside a block,
not outside of it.
